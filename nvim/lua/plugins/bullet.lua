-- In your plugins configuration file (e.g., ~/.config/nvim/lua/plugins/init.lua)
return {
  -- Other plugins...

  {
    "bullets-vim/bullets.vim",
    -- You can add optional configurations here
    -- For example, to set specific file types or disable default mappings:
    -- config = function()
    --     vim.g.bullets_enabled_file_types = { 'markdown', 'text' }
    --     vim.g.bullets_set_mappings = 0
    -- end,
  },

  -- Other plugins...
}
